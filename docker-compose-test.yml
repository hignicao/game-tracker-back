services:
  postgres:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - 5433:5432
    env_file:
      - .env.test

  redis:
    image: redis
    container_name: redis
    ports:
      - 6378:6379
    env_file:
      - .env.test

  node:
    container_name: node
    build: .
    image: gametracker/back
    ports:
      - 5000:5000
    depends_on:
      - postgres
      - redis
    env_file:
      - .env.test